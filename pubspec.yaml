name: pro_app
description: "LUMO Professional Partner — Verified Service Providers"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.6.0
  shared_preferences: ^2.3.5
  geolocator: ^13.0.4
  web_socket_channel: ^3.0.2
  intl: ^0.20.2
  google_maps_flutter: ^2.10.0
  image_picker: ^1.1.2
  file_picker: ^8.1.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
